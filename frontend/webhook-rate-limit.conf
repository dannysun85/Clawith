# Loaded by nginx inside the http context before the rendered server config.
# The zone is keyed by the binary source address, so rotating URL tokens cannot
# create unbounded ingress buckets.
limit_req_zone $binary_remote_addr zone=webhook_ingress:10m rate=10r/s;
