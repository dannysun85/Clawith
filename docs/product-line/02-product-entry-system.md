# 产品入口体系事实基线

- 状态：`active-design-baseline`
- 日期：2026-07-31
- 核心决策：任务优先，员工其次；普通用户从业务结果进入，平台内部完成能力与执行者路由

## 1. 当前入口事实

| 当前入口 | 代码事实 | 当前主要职责 | 问题 |
|---|---|---|---|
| `/` | `App.tsx` 重定向到 `/dashboard` | 公司概况 | 不是用户“今天要完成什么”的入口 |
| Onboarding | 创建私人助手后进入 `/plaza?tour=company...`，导览结束进入助手聊天 | 注册、创建助手、公司导览 | 首次任务被导航导览打断；助手与员工混淆 |
| Dashboard | 展示 Agent、活动、Token/任务概况和新增 Agent | 运营概览 | 同时承担员工管理入口，和工作入口重叠 |
| Plaza | 路径仍为 `/plaza`，实际是团队/我的 Experience Library | 经验沉淀 | 名称仍像社交广场；员工发现另藏在 Talent Market 弹窗 |
| Groups | 群组树、会话、消息、成员、群 Workspace | 可见多人协作 | 与 Agent A2A、公司目录和任务责任人边界不够直观 |
| Agent chat | 对话、Task、媒体快捷入口、Deliverable、Workspace | 指定员工执行现场 | 承载过多入口和设置；必须先知道找哪个 Agent |
| Enterprise | 成员、组织、Tools、Skills、审批、审计、订阅等 | 公司控制面 | 内容庞大，部分平台控制面已开始跳转 SaaS Admin |
| Subscription | `/account/subscription` + Enterprise Subscription | 用量、账单、升级 | 两个表面存在职责重叠 |
| Deliverable | Agent chat 内工作说明、结果卡和右侧详情 | 正式交付 | 不是独立可发现入口，也没有跨 Agent 结果索引 |

## 2. 目标入口定义

### 2.1 工作台：默认任务入口

名称确定为 `工作台`，不叫“助手”或“超级助手”。它是任务入口与工作索引，不是一个拟人角色。

工作台负责：

- 接收自然语言、文件、图片和语音输入；
- 提供按业务结果组织的快捷入口；
- 识别任务类型并补齐关键约束；
- 提议由私人助手、临时专家、数字员工或 Group 处理；
- 展示能力、费用和审批预检摘要；
- 聚合 `等待我处理`、`进行中`、`最近完成` 和常用工作；
- 深链回真实 Agent/Group 会话、Deliverable 详情和 Workspace Artifact。

工作台不负责：

- 自己成为一个 Agent；
- 复制 Task/Run/Deliverable 状态；
- 暴露 Provider、模型、Skill 或 Tool 选择；
- 新建第二套调度器或文件系统。

### 2.2 我的助理：默认私人协调关系

- 导航固定显示 `我的助理`，自定义名称作为副标题。
- 用于备忘、跟进、草拟、轻量计划和任务派发。
- 它可以帮用户找工作和派发工作，但正式任务仍落到 Task/Run/Deliverable。

### 2.3 数字员工：长期执行角色

- 展示公司/私有/自定义的长期 Agent 员工。
- 用户在需要长期责任、触发器、渠道或持续记忆时才进入招聘/创建流程。
- 员工卡展示业务职责和可完成的工作，不把原始 Skill 列表当商品描述。

### 2.4 Group：多人协作

- 负责多个人和 Agent 的共享会话、可见分工、交接与审批。
- 每项正式工作仍要有一个责任主体和稳定 Task/Run ID。

### 2.5 Workspace：工作现场

- Workspace 是执行中的文件、草稿、来源、脚本、中间产物和版本现场。
- Agent Workspace 和 Group Workspace 保留现有所有权；工作台只建立深链和索引。
- Workspace 不是最终交付清单，也不等于公司网盘。

### 2.6 Deliverable：正式产物

- Deliverable 是有工作说明、版本、质量检查、审批、交付状态和 Artifact 的正式结果。
- 产出者在自己的时间线里给出结果；右侧详情承载检查和交付动作；输入框不显示已完成产物。

### 2.7 广场：经验与员工发现

目标广场包含两个明确分区：

- `经验库`：已审核发布、可供人和 Agent 复用的公司经验；
- `员工市场`：发现公司模板、内置角色和可招聘数字员工。

当前 `/plaza` 只实现经验库，员工发现仍是 `TalentMarketModal`。第一阶段可保留路径和弹窗，但必须在信息架构上给出统一入口，不能继续让两个“发现”概念分散。

## 3. 目标任务路径

```text
用户表达意图
  → 工作台/我的助理识别工作类型
  → 补齐必要约束
  → 平台提议责任主体
  → 能力/权限/费用/审批预检
  → 用户确认工作说明
  → 创建稳定 Task/Deliverable 与 Run
  → 在 Agent 或 Group 工作现场执行
  → 产物进入 Workspace 并登记 Artifact
  → 检查、修改、批准与交付
  → 可选择沉淀为个人快捷工作、公司模板或 Experience
```

用户仍可以直接进入某个 Agent chat 或 Group 发起任务；这些是专业入口，不是唯一入口。所有入口最终必须进入同一套持久业务对象。

## 4. 执行者选择规则

| 用户意图 | 默认责任主体 | 说明 |
|---|---|---|
| 备忘、跟进、草拟、个人安排 | 私人助手 | 保持私人上下文 |
| 一次性调研、一次 PPT、一次广告视频 | 临时专家 | 不要求招聘长期员工 |
| 持续运营、每天汇报、长期客户跟进 | 数字员工 | 需要记忆、Trigger、Channel、KPI |
| 多方讨论、交接、联合审批 | Group | 过程需要对成员可见 |
| 已明确点名某员工 | 该数字员工 | 仍执行能力和费用预检 |

## 5. WorkBuddy 对比所得结论

先前实际观察到的 WorkBuddy 使用路径是任务优先：用户先描述结果，系统再澄清、确认和执行，Skill/Tool/Provider 不占据普通用户的第一步。这支持 Astra 隐藏内部能力结构、增加工作台的方向。

该观察不能被夸大为 WorkBuddy 已证明完整商业交付；先前测试没有形成可独立验收的最终交付物。Astra 的差异化应是把任务优先入口与现有企业 Agent、Group、权限、Credits、Workspace 和可审计交付结合起来，而不是复制一个通用聊天首页。

## 6. 导航层级目标

```text
工作
  工作台
  我的助理
  Groups

组织
  数字员工
  仪表盘
  OKR
  广场（经验库 / 员工市场）

治理（按角色显示）
  企业设置
  套餐与用量
  SaaS 管理
```

Deliverable 和 Workspace 不需要成为与工作台同级的孤立一级菜单：

- 工作台提供跨运行时结果索引；
- Agent/Group 页面提供工作现场；
- Deliverable 详情提供正式交付；
- Workspace 提供文件和中间过程。

## 7. 无破坏迁移顺序

1. 先分离导航中的“我的助理”和“数字员工”，不改任何 Agent ID 或路由。
2. 新增只读 `/work` 聚合页，读取现有 Task、Run、Deliverable、Approval 和 Artifact。
3. Onboarding 完成后默认进入 `/work`；助手创建失败时仍允许进入工作台并显示可恢复状态。
4. 保留 `/dashboard`、`/plaza`、`/agents/:id/*`、`/groups/*` 和现有深链。
5. 把 Talent Market 作为广场“员工市场”分区复用，而不是重写招聘流程。
6. 只有在新工作流证明功能等价后，才把聊天中的图片/视频/音乐快捷键归入“快速生成”。

## 8. 完成标准

- 新成员进入系统后能在不理解 Agent/Skill/Tool 的情况下发起首个任务。
- 用户能明确区分私人助手、长期员工、临时专家、Group、Workspace 和 Deliverable。
- 任意工作都能从工作台回到真实责任人、Run、Artifact、审批和交付状态。
- 原有深链、权限、会话和 Workspace 文件不因入口重组而失效。
