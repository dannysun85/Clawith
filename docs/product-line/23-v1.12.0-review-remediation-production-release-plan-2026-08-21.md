# Astra v1.12.0 全量审查修复与生产发布计划

日期：2026-08-21
状态：执行基线
主责：本次 Codex 任务是唯一发布切流主责
审查输入：`/Users/sun/.cursor/projects/Users-sun-Documents-PythonProject-Clawith/v1.12.0-dev-team-review.md`

## 1. 目标与发布边界

本轮目标不是“让测试变绿后上线”，而是把审查报告的 39 项问题逐项形成可验证结论，修复所有代码与产品合同缺口，处理生产运行风险，冻结不可变候选，并且只在全部发布门禁通过时将 v1.12.0 蓝绿切流到生产。

本轮必须分别记录以下状态，任何一项不得替代另一项：

1. `code_exists`：代码和配置合同存在；
2. `tests_pass`：针对性和全量自动化测试通过；
3. `local_business_flow_proven`：真实浏览器、真实本地 PostgreSQL、持久化状态与 Artifact 链路通过；
4. `candidate_frozen`：干净工作树、不可变 commit、版本和包身份一致；
5. `deployment_verified`：正式脚本完成蓝绿部署且公开版本身份一致；
6. `production_business_flow_proven`：生产登录、权限、聊天、工作台、计费语义、拓扑和恢复路径通过；
7. `provider_verified`：仅用于实际启用的 Provider。生产继续 `BILLING_PROVIDER=manual` 时，不以本地微信 smoke 冒充微信生产可用；
8. `production_verified`：发布后监控、数据库、容器、DNS 和回滚门全部通过。

默认发布边界：

- v1.12.0 继续销售登录/公司/成员/权限、非 CEO 数字员工、Direct Chat、文件/工作区、工作台海报/视频/PPT v1、人工账单、组织拓扑；
- CEO 协调、Creative V2、微信 Native、Code/AgentBay、自动 OKR、Heartbeat、legacy schedule、抖音直发、三人商用品质门继续关闭，关闭状态必须诚实可见；
- 报告和表格在没有正式 workflow 时只能是 Brief，不得标记为正式交付；
- 不在版本列车中并库、改生产活卷或自动清理无法证明归属的历史数据；
- 不自动为所有租户创建或启用 CEO。只提供可审计 migration preview 和独立 canary 启用路径；
- 不在本轮创建真实微信订单、扣费或退款。启用微信仍需独立的商户/provider 授权门。

## 2. 当前证据基线

- 本地分支：`codex/v1.12.0-local-release`；本计划建立前 HEAD 为 `2186a3839fa7bc969cdb1c787e371bc369c58ace`；
- 被审查候选：`907132bff769fe55675a8b3be6c77433fee9a74f`；
- 本地 `backend/VERSION` 与 `frontend/VERSION` 均为 `1.12.0`；
- 本计划建立时 Alembic head 为 `backfill_deliv_audit_tenant`；当前待冻结工作树已线性推进到唯一 head `manual_order_decisions`（新增 `billing_effect_receipts`、`manual_order_decisions`）；
- 公开生产仍为 `1.11.42 / b5f7b20214b187ab53f88496cbf58cb2f335491d / 20260820-050718-b5f7b20214b1-25cab9b9-astra-saas`；
- 生产数据库仍停在 `ceo_orchestrator_settings`，距离候选 head 有 `ceo_coordination_mode`、`widen_creative_brief_schema`、`backfill_deliv_audit_tenant` 三个 revision；
- 审查报告包含 4 个 P0、12 个 P1、17 个 P2、6 个 P3；其中 34 项需要代码/产品修复，5 项属于生产门禁或明确产品决策；
- 生产已直接复现 13 项：P0-2、P0-3、P0-4、P1-1、P1-2、P1-3、P1-6、P2-1、P2-4、P2-5、P2-7、P2-8、P3-3；其余不得因生产旧版本尚未触发而忽略；
- 生产存在 stale provider reservation、manual pending order、历史 CEO 形态和运行错误。它们必须先做只读归因，再执行幂等、可回滚且不猜测 Provider 结果的修复。

## 3. 正确修复顺序

### Phase 0 — 身份、范围与验收矩阵锁定

目的：保证审查源码、当前候选、部署包和生产运行身份不漂移。

验收：

- 保存本轮 39 项问题矩阵，每项只有 `open / fixed / decision_closed / external_gate / production_verified` 五种状态；
- 重新记录 Git 状态、HEAD、版本、Alembic heads、公开 `/api/version`、生产 `current` 指针、容器身份和数据面 DNS；
- 明确本轮不打开的 flags、生产数据非目标和回滚条件；
- 任何后续新发现进入台账，不通过口头说明丢失。

### Phase 1 — 产品真相与 P0 误导修复

先修用户能看见却不会发生、以及付费口径错误的问题，避免后续业务流建立在错误承诺上。

- P0-1：重写 v1.12.0 发布叙事；CEO 模板声明不可普通招聘，CEO 仅为独立 canary；
- P0-2、P1-1、P1-2、P3-2：年付 Credits 改为“每周期/每年一次”，补完整中英 i18n，删除 `price_cents * 10` 假年价路径，锁定 yearly 恰好一笔 grant；
- P0-3、P2-5：API 暴露 server-owned OKR automation availability；平台关闭时开关只读/保存 fail-closed，Dashboard 显示可解释空态，trigger 不再把未执行冒充成功；
- P0-4：API 暴露 Heartbeat availability；平台关闭时禁止保存 enabled，并在 Agent 设置展示原因；
- P1-3、P3-1：报告/表格明确为 Brief-only，移除正式交付 CTA；
- P1-4、P1-5、P1-6、P1-7、P2-6、P2-10：所有关闭能力在 UI、API 和发布说明中使用同一可用性合同，不展示可配置但必失败的入口；
- P2-1、P2-7、P2-8、P3-3：统一 Astra/Agent Company/数字员工/团队知识命名并补齐中英文案。

验收：关闭 flags 的真实浏览器中不存在静默成功、假承诺、中文 fallback、不可执行的正式 CTA 或在线微信支付暗示。

### Phase 2 — 权限、CEO 与能力治理修复

在自动化和委派能力之前先把 authority 边界锁死。

- P1-8：CEO enable 向导必须显式确认节奏；协调模式要求有效成员；观察空壳必须明确说明；
- P2-2：治理者看到 migration preview、分类、拟创建/关联关系和不修改数据的预览入口；
- P2-3：`soul.md` / `memory.md` 的 manage-only 合同在 UI 中显示明确权限原因；
- P2-9：晨会/周会使用各自名称和 trigger；
- P2-11：观察者 CEO 的工具 schema 不再诱导 `task_delegate`，协调模式才暴露受管 delegate；
- P2-13：普通员工 Directory 只看到安全能力类别，治理者/受管 CEO 才能看到高风险工具名；
- P2-17：CEO 模板 lifecycle 明确不可招聘，列表过滤与直接创建 409 双重保护；
- P3-4：明确拒绝通配符 `*`，只接受精确 tenant/agent UUID，并补测试与文档；
- P3-5：成员只请求 member-safe CEO status，不预取 governor-only brief/settings，消除 403 闪错。

验收：owner/admin/member/use 四类身份分别跑 API 与浏览器矩阵；任何能力需要 Tool、凭证、tenant、审批、配额和 receipt 才能被描述为可执行。

### Phase 3 — 架构正确性、幂等与规模修复

- P1-11、P2-4：拓扑 execution 查询按时间窗和每 Agent/租户硬 cap；轮询至少 15 秒并在页面隐藏/错误时暂停；治理计数、执行状态和健康状态分离；用真实 PostgreSQL 做大样本查询预算；
- P1-12：processed webhook 重放必须携带 `order_id` 并可幂等补做 subscribe effects；新增 paid-but-effects-incomplete reconciliation；
- P2-12：退款矩阵区分 full/partial、subscribe/topup、current/historical plan、可用/预留/已消费 Credits；部分退款不得粗暴取消整个当前订阅；
- P2-14：新协议缺 `launchable` 必须 fail-closed；兼容只允许显式旧协议版本；
- P2-15：远程 Close 失败进入可观察、可重试状态，不伪造本地 canceled，也不无限无告警 pending；
- P2-16：Tool schema 建立单一来源，seeder 与 builtin projection 有相等性测试；
- P3-6：Memory 改为 section-aware 有界压缩，保留标题/制度段标识和最近修正；超限必须可观察，禁止静默丢失中间制度。

验收：并发、重复 webhook、退款、关单失败、千级 topology、缺字段、目录漂移和超长 Memory 均有回归测试；不得以 SQLite 代替关键 PostgreSQL 证明。

### Phase 4 — 部署合同与数据面保护

- P1-9：继续使用正式脚本的只读 DNS 唯一性门与 `--no-deps`；不得改生产活卷或并库；
- P1-10：prod compose 与 deploy script 将 CEO、CEO coordination、Creative V2 的 enabled 设为字面 `false`、allowlist 设为空，并在候选容器内核验；
- 所有生产关闭 flags 形成单一 contract test；包内 version/commit/release id 可追溯；
- Alembic 从生产 revision 到唯一 head 的升级、降级边界和再升级在隔离 PostgreSQL 完成；
- 回滚继续指向上一不可变 release，数据库 migration 必须 expand-only，旧代码可运行。

验收：任何 DNS 多地址、依赖容器启动、卷身份漂移、迁移多 head、flag 非预期打开或候选身份不一致都立即停止发布。

### Phase 5 — 生产历史数据与运行问题治理

先只读分类，再按证据执行；禁止批量猜测 Provider 状态。

- stale `provider_inflight`：逐条检查 Provider receipt、远程状态和账本；只有确定 terminal 且符合 exactly-once 合同才释放或结算；未知保持冻结并升级人工处置；
- `settlement_ready`：通过幂等 settlement/reconciliation 补做，核对公司余额和 credit transaction；
- manual pending orders：区分仍有效、过期、已人工履约、测试数据；提供可审计处置入口，不直接删除；
- production issues：按根因、影响租户、首末发生时间、版本和是否仍可复现归并，修复相关代码后再关闭；
- 历史 CEO：为 51 个活跃租户生成 migration preview；不得自动把名字含 CEO 的普通 Agent 提升为 system CEO，也不得默认创建；
- heartbeat/OKR 历史设置：当平台总闸关闭时在 UI 标记 suspended，不篡改租户原始意图。

验收：每条生产写操作都有目标、非目标、前后值、幂等 key、操作者和回滚/补偿记录。无法确定的记录进入明确人工队列，不被“清零”。

2026-08-21 完成证据：生产只读归因确认 Credits reserved 无漂移；13 条 LLM Provider 持有和 6 条媒体 Provider 债务因缺少终态凭证保留在人工队列；27 条 manual pending 未被猜测性修改。51 个活跃租户的完整 storage-aware CEO preview 为 39 个 contaminated、2 个 clean-adoptable-review、10 个 none，未自动创建或接管。新增人工订单幂等 receipt、可回滚处置 API/UI、匿名生产治理 inventory 和正式部署 drift 门；真实 PostgreSQL migration/并发 smoke 通过。详细证据保存在 `.omx/ultragoal/g5-production-governance-evidence.md`。

### Phase 6 — 全量本地与业务流验证

自动化层：

- changed-file Ruff、类型检查、前端 lint/test/build、backend targeted tests、backend full pytest；
- Alembic single-head、PostgreSQL migration smoke、deploy contract、flag contract、DNS unit/fixture；
- security/permission、billing concurrency、webhook/reconciliation、workforce scale、CEO mode、Creative v1/v2 fail-closed 回归。

真实业务流层：

- 注册登录、创建公司、员工加入、owner/admin/member/use 权限；
- Dashboard、工作台、协作群组、员工拓扑、Direct Chat、文件/Workspace、团队知识；
- 非 CEO 招聘、CEO migration preview 与关闭态；
- OKR/Heartbeat/schedule/Code/Douyin/Creative V2 关闭态；
- 工作台 v1 海报/视频/PPT 从 UI → API → durable state → runtime → Artifact → review → user outcome；
- 报告/表格 Brief-only；
- manual 订阅订单、年/月周期、Credits grant/consume/release/refund；
- 浏览器桌面/移动端、中文/英文、刷新/断线/恢复、无 console error。

验收证据必须绑定当前 HEAD、数据库、浏览器录屏/截图、请求回执和 Artifact hash。

2026-08-21 完成证据：backend 全量 `5068 passed`；生产部署/验收身份合同组合测试 `220 passed`；frontend Node `193 passed`、Vitest `41 files / 228 passed`，production build `6470 modules transformed`，`npm audit` 为 0；真实 PostgreSQL fresh/history/downgrade/re-upgrade、billing paid effects、manual-order、WeChat stub、workforce 以及专用验收身份并发幂等/撤权/清理 smoke 均通过。生产形态 Docker + 浏览器完成公司创建/加入、owner/admin/agent_admin/member/platform 权限、工作台、协作群组、拓扑实时状态、Direct Chat、分作用域 Workspace、团队知识、PPT 生成—两次修改—批准—交付、manual order exactly-once Credits、关闭能力、桌面/移动、中英切换和真实 backend 断线—降级—原身份恢复。PPT artifact `619c2657-aca9-448e-844a-ba6d8f3386f8` 的 SHA-256 为 `85bfeb0ae858df100532046688519e71b793eceae4a45ffc9a1a224d649560b0`；manual order `7a96e32e-8d5c-4407-855c-899953549d9a` 只有一条决策和一条 `+10000` grant。完整证据见 `.omx/ultragoal/g6-local-business-flow-evidence.md`；该证据仍不等于 candidate/provider/deployment/production proof。

### Phase 7 — 最终清理、独立审查与候选冻结

- 只对本轮改动运行 ai-slop-cleaner，复跑全部受影响测试；
- 独立 code-reviewer 必须 `APPROVE`，独立 architect 必须 `CLEAR`；任何 `WATCH`、`REQUEST CHANGES` 或缺失证据均新增修复故事；
- 更新 39 项矩阵、release notes、changed files、测试和未启用外部门；
- 工作树干净后创建不可变 commit，重新从该 commit 跑发布前门禁；
- 本地候选 SHA、镜像/包、version 和 release id 必须一致。

2026-08-21 历史证据：`2427570de1790a590dc711b14586cd33704c2026` 曾完成当时
源码快照的全量门和独立审查。Phase 8 只读预检随后确认旧 Release QA 凭据不可登录、
普通成员和专用 tenantless 平台操作员缺失；为避免复用客户/多租户管理员或跳过真实
smoke，现已加入专用验收身份的严格边界、exactly-once 准备、候选 smoke 后撤权和
PostgreSQL 并发验证。因此该 SHA 只保留为历史候选证据，不再作为最终发布候选；
新增内容完成全量复验与独立审查后必须重新提交并冻结新的唯一 SHA。

### Phase 8 — 条件式生产发布、验证与回滚

只有 Phase 0–7 全部通过才执行：

1. 再次声明唯一切流主责，记录 active/previous slot、生产 `/api/version`、current symlink、容器和数据面 DNS；
2. 如专用验收身份需要准备，先取得生产安全写授权并按 `.agents/workflows/manage-production-smoke-principals.md` 提供精确 Release QA 租户和 operation UUID；默认不修改身份；
3. 运行 `scripts/deploy-astra-production.sh` 正式蓝绿路径，不手工 `docker compose up`；
4. 候选槽启动后先验证 migration、flags、容器健康、DNS、单 worker 和 release identity，再切流；
5. 公开两个域名的 version/commit/release id 必须匹配不可变候选；
6. 用批准的生产 smoke tenant 跑登录、权限、聊天、拓扑、工作台、manual 计费语义、关闭态和 Credits exactly-once；临时平台操作员必须在切流前完成撤权和 token 回收；
7. 检查 API/worker/frontend 日志、production issues、reservation、订单、重启/OOM、延迟和错误率；
8. 任何身份不一致、迁移异常、数据面歧义、5xx、Credits 漂移、权限越界或关键业务流失败，立即按正式脚本回滚到 previous slot；临时平台撤权失败时必须 fail-closed 保持维护态，拒绝恢复公开流量，直到撤权得到验证；
9. 发布完成后仍将微信 Native、CEO/Creative V2 canary、Code 等标记为 `external_gate`，不得宣称已生产验证。

## 4. 39 项关闭矩阵

状态口径：`fixed` 表示实现与自动测试均已关闭；`decision_closed` 表示产品明确不提供该承诺并以关闭态验证；`external_gate` 表示代码随包但启用/生产证明必须另行授权。候选冻结前没有任何一项标记为 `production_verified`。

| ID | 状态 | 本轮关闭方式与证据 | Phase |
|---|---|---|---:|
| P0-1 | `fixed` | 发布叙事改为 dormant；CEO template `not_recruitable`，普通创建 409 | 1、2 |
| P0-2 | `fixed` | Credits 按周期/年一次文案；yearly 只有一笔幂等 grant | 1、3 |
| P0-3 | `fixed` | OKR server-owned availability；总闸关时 UI/API/trigger fail-closed | 1 |
| P0-4 | `fixed` | Heartbeat availability；平台关闭时控件只读且保存被拒绝 | 1 |
| P1-1 | `fixed` | `enterprise.subscription.*` 中英键完整性合同 | 1 |
| P1-2 | `fixed` | 升级立即、降级周期末；删除“随时切换”误导 | 1 |
| P1-3 | `decision_closed` | Report 只保存 Brief，无正式生成/交付 CTA | 1 |
| P1-4 | `external_gate` | Creative v2 叙事收口，prod compose/deploy 字面关闭且 allowlist 空 | 1、4、8 |
| P1-5 | `external_gate` | manual 产品真相；WeChat readiness fail-closed，真实商户单未授权 | 1、8 |
| P1-6 | `external_gate` | Code/AgentBay 租户可见面隐藏/禁用；普通发版强制关闭 | 1、4、8 |
| P1-7 | `decision_closed` | legacy schedule 执行关闭，旧 UI 移除；不以无 lease 路径冒充可运行 | 1 |
| P1-8 | `fixed` | CEO enable 必须显式 cadence 或 observer-only；会议 cadence 必须有成员；手动启动同样双门 | 2 |
| P1-9 | `external_gate` | DNS 唯一性、活卷 identity、所有槽位 `--no-deps` 为发布硬门；G8 再证生产 | 4、8 |
| P1-10 | `fixed` | CEO/coordination/Creative v2/quality flags 在 compose 与 deploy 中字面 false | 4 |
| P1-11 | `fixed` | topology 24/Agent、2000 全局硬 cap；15 秒轮询、错误退避、后台暂停 | 3 |
| P1-12 | `fixed` | `billing_effect_receipts` lease/attempt fence；重复 webhook/reconciliation 可补效果不重发 Credits | 3 |
| P2-1 | `fixed` | 登录品牌、Agent Company 与数字员工术语合同统一 | 1 |
| P2-2 | `fixed` | governor-only、tenant-scoped、无正文、零 mutation migration preview | 2 |
| P2-3 | `fixed` | soul/memory/skills 修改、锁、恢复均要求 manage；use-only UI 解释 | 2 |
| P2-4 | `fixed` | 健康、执行、work、analytics 三类状态/计数分离 | 3 |
| P2-5 | `fixed` | Dashboard 与 `/okr` 保留可解释关闭空态和治理入口 | 1 |
| P2-6 | `external_gate` | 抖音连接/发布包与 direct publish capability 分离；直发继续关闭 | 1、8 |
| P2-7 | `fixed` | 团队知识/发现命名与空态收口，不再把空壳当能力 | 1 |
| P2-8 | `fixed` | SaaS 媒体 route 状态、readiness 与警告完整中英合同 | 1 |
| P2-9 | `fixed` | 晨会/周会 trigger 和名称分离，共享中性“CEO 会议室”；observer/manual 双门 | 2 |
| P2-10 | `decision_closed` | v1 结构+creator approval 为发布门；三审 managed quality 默认关闭 | 1、4 |
| P2-11 | `fixed` | observer schema 仅 `consult` 且移除 delegation contract；coordinator 才暴露 governed delegate | 2 |
| P2-12 | `fixed` | full/partial、subscribe/topup、available/reserved/consumed refund 矩阵与回归 | 3 |
| P2-13 | `fixed` | ordinary Directory 只给安全类别；精确 Tool/风险细节按 authority 裁剪 | 2 |
| P2-14 | `fixed` | v2 新协议缺 `launchable` 必须 fail-closed | 3 |
| P2-15 | `fixed` | Provider close 失败进入 durable retry/operator-review/production issue | 3 |
| P2-16 | `fixed` | model/runtime/seeder 使用 canonical immutable builtin source；完整 seed/schema 相等性测试 | 3 |
| P2-17 | `fixed` | CEO lifecycle `not_recruitable`，列表过滤与直接创建 409 双保护 | 2 |
| P3-1 | `decision_closed` | Spreadsheet 只保存 Brief；没有正式 workflow 时不展示交付承诺 | 1 |
| P3-2 | `fixed` | 删除前端推导年价，价格只来自后端 SKU | 1 |
| P3-3 | `fixed` | Apply/session expired 与相关设置文案完整中英 | 1 |
| P3-4 | `fixed` | CEO rollout allowlist 只接受精确 UUID，`*`/`all` fail-closed | 2 |
| P3-5 | `fixed` | member 只请求 safe status；settings/brief/meeting 按 authority 懒加载 | 2 |
| P3-6 | `fixed` | Memory 有界保留 identity/制度头与最新 correction tail；截断有标记 | 3 |

## 5. 最终完成定义

只有同时满足以下条件，本轮目标才可完成：

- 39 项全部为 `fixed / decision_closed / external_gate / production_verified`，不存在无解释 open；
- 所有代码修复有自动测试，所有用户流程有真实浏览器和 durable state 证据；
- 全量 backend/frontend/build/migration/governance/deploy contract 通过；
- 独立代码与架构审查干净；
- 候选不可变、工作树干净、版本和包身份一致；
- 正式脚本成功部署，生产业务流、数据面、容器、日志与监控通过；
- 未启用能力继续明确标记为关闭或 external gate，没有因本次发版扩大授权；
- 若任一生产门失败，完成回滚并将目标保持 active/blocked，绝不把“已回滚”写成发布成功。
